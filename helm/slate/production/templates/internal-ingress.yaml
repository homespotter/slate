apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.environmentPrefix }}-{{ .Values.appName }}-internal-ingress
  namespace: {{ .Release.Namespace }}
  annotations:
    alb.ingress.kubernetes.io/listen-ports: {{ .Values.ingress.internal.alb.listenPorts | quote }}
    alb.ingress.kubernetes.io/unhealthy-threshold-count: {{ .Values.ingress.internal.alb.unhealthyThresholdCount | quote }}
    alb.ingress.kubernetes.io/success-codes: {{ .Values.ingress.internal.alb.successCodes }}
    alb.ingress.kubernetes.io/target-type: {{ .Values.ingress.internal.alb.targetType }}
    alb.ingress.kubernetes.io/healthcheck-path: {{ .Values.ingress.internal.alb.healthcheckPath }}
    alb.ingress.kubernetes.io/healthcheck-port: {{ .Values.ingress.internal.alb.healthcheckPort }}
spec:
  ingressClassName: {{ .Values.ingress.internal.ingressClassName }}
  rules:
    {{- range $.Values.hostsInternal }}
    - host: {{ . }}
      http:
        paths:
          - path: {{ $.Values.ingress.pathPrefix }}
            pathType: Prefix
            backend:
              service:
                name: {{ $.Values.environmentPrefix }}-{{ $.Values.appName }}-service
                port:
                  number: {{ $.Values.service.ports.port }}
    {{- end }}